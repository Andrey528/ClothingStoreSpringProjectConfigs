eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URI:http://localhost:8085/eureka}
  instance:
    preferIpAddress: true

api:
  basic-uri:
    resource-server: http://localhost:8081
    payment-server: http://localhost:8082